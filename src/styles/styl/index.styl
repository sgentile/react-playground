@import '~font-awesome/css/font-awesome.css';
@import 'App';

body {
  margin: 0;
  padding: 0;
  font-family: sans-serif;
}

ul {
  list-style: none;
}

form button {
  margin: 10px;
}

.todo-list {
  display: flex;
  align-items: left;
  justify-content: center;
}